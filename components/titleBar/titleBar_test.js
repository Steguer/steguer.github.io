'use strict';

describe('stevenGerardCV.titleBar module', function() {

  beforeEach(module('stevenGerardCV.titleBar'));

  describe('titleBar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var titleBarCtrl = $controller('titleBarCtrl');
      expect(titleBarCtrl).toBeDefined();
    }));

  });
});
